# ========================================
# 🚂 RAILWAY ENVIRONMENT VARIABLES
# ========================================
# Copia estas variables en Railway Dashboard > Variables
# Railway auto-genera las variables ${{VARIABLE}} con los valores reales

# ========================================
# 🌍 ENTORNO
# ========================================
NODE_ENV=production

# ========================================
# 🗄️ POSTGRESQL (Auto-generadas por Railway)
# ========================================
# Railway PostgreSQL addon genera estas automáticamente
DB_HOST=${{PGHOST}}
DB_PORT=${{PGPORT}}
DB_USER=${{PGUSER}}
DB_PASSWORD=${{PGPASSWORD}}
DB_NAME=${{PGDATABASE}}

# ========================================
# 🔴 REDIS (Auto-generadas por Railway)
# ========================================
# Railway Redis addon genera estas automáticamente
REDIS_HOST=${{REDIS_HOST}}
REDIS_PORT=${{REDIS_PORT}}
REDIS_PASSWORD=${{REDIS_PASSWORD}}

# ========================================
# 🔐 JWT CONFIGURATION
# ========================================
JWT_SECRET=tu-secreto-super-seguro-para-produccion-cambiar-esto
JWT_EXPIRES_IN=7d
JWT_ALGORITHM=HS256

# ========================================
# 🚪 PORT
# ========================================
# Railway asigna automáticamente, pero puedes especificar
PORT=${{PORT}}

# ========================================
# 📝 NOTAS IMPORTANTES
# ========================================
# 1. Las variables ${{VARIABLE}} son reemplazadas automáticamente por Railway
# 2. NO necesitas crear manualmente PGHOST, PGPORT, etc. Railway las genera
# 3. JWT_SECRET DEBE cambiarse a algo único y seguro en producción
# 4. NODE_ENV=production es crucial para que las migraciones funcionen
# 5. No incluyas comillas en los valores en Railway Dashboard
